name: Android CI/CD

on:
  push:
    branches: [ "main" ]
    paths:
      - 'app/**'
      - 'build.gradle.kts'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'app/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build with Gradle (Debug)
      run: ./gradlew assembleDebug

    - name: Run Unit Tests
      run: ./gradlew testDebugUnitTest

    # Production Release Build Preparation (Optional - usually requires Secrets)
    # - name: Setup Keystore
    #   run: |
    #     echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > app/keystore.jks
    #     echo "keystore_password=${{ secrets.KEYSTORE_PASSWORD }}" > keystore.properties
    #     echo "key_alias=${{ secrets.KEY_ALIAS }}" >> keystore.properties
    #     echo "key_password=${{ secrets.KEY_PASSWORD }}" >> keystore.properties
    
    # - name: Build Release APK
    #   run: ./gradlew assembleRelease
